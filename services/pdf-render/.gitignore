node_modules
dist

# Build-time-only fixture generated by scripts/generate-priming-fixture.mjs
# (run inside the Dockerfile's `build` stage) — never meant to be committed,
# but the script writes it to the project root, so it can land in a working
# tree if run manually/locally outside Docker.
priming-exam.tex
