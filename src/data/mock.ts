/** Static lists — deprecated, data now comes from enamed_* tables in Supabase. */
// Kept as fallback only — prefer useEnamedSpecialties() and useEnamedInstitutions() hooks.
export const SPECIALTIES: string[] = [];
export const INSTITUTIONS: string[] = [];
